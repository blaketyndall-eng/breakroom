---
/**
 * /lost-found — DEPRECATED (308 redirect to /newsstand/classifieds).
 *
 * Per SITE-ARCHITECTURE.md, Lost & Found folds into SleepNews as a
 * Classifieds section — same data via getPublicLostObjects(),
 * SleepNews chrome wrapping the police-blotter format.
 * PR 71 retires this route.
 */
return Astro.redirect('/newsstand/classifieds', 308);
---
