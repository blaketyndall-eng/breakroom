---
/**
 * /after-hours — DEPRECATED (308 redirect to /void).
 *
 * The legacy After Hours page predates the Void Signal homepage at
 * /void. SITE-ARCHITECTURE.md flagged it for replacement; PR 71 retires
 * it. The `afterhours` section is preserved in worldMap.ts because
 * /sign-the-wall still uses it — only this page retires.
 *
 * 308 preserves request method semantics. The original module's
 * SecretTrigger fired on `visit:/after-hours`; that trigger is now
 * dead — the Void homepage's own visit triggers replace it.
 */
return Astro.redirect('/void', 308);
---
